# Railway deployment config for Milens GitHub App
# Docs: https://docs.railway.app

# Railway auto-detects Dockerfile — no build config needed

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
